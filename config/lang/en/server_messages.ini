# configuracao de mensagens do servidor
# server messages config

# setup phase (easygui dialogs)

run which experiment = "Run which experiment?"

cancel server = "Cancelled server!"

create new group = "-> create new group!"

run which group = "Run which group?"

type group name = "Type group name."

new group = "New group"

group exists = 'Group {} exists already. Try again.'

# messages passed to clients

server full = 'Sorry, server is full. Try again later.'
